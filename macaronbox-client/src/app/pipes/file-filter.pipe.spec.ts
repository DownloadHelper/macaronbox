import { FileFilterPipe } from './file-filter.pipe';

describe('FileFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FileFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
